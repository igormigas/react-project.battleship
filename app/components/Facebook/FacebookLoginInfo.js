import React from 'react';

const facebookLoginInfo = (props) => (
	<div className="fbInfoBox">
		<span className="fbInfoMsg">Please log in with Facebook to start the game.</span>
	</div>
);

export default facebookLoginInfo;
